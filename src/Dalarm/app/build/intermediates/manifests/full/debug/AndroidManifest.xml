<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.seon.alarm"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="18"
        android:targetSdkVersion="25" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <!-- splash activity -->
        <activity
            android:name="kr.seon.alarm.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- MainActivity가 사라져서 에러가 나서 다시 추가함 -->
        <activity android:name="kr.seon.alarm.MainActivity" />
        <activity android:name="kr.seon.alarm.removeActivity" >
            <intent-filter>
                <action android:name="AlarmRemove" />
            </intent-filter>
        </activity>

        <receiver
            android:name="kr.seon.alarm.AlarmReceiver"
            android:exported="true" >
            <intent-filter>
                <action android:name="AlarmReceiver" />
            </intent-filter>
        </receiver>

        <activity android:name="kr.seon.alarm.AlarmNotification" />
        <activity android:name="kr.seon.alarm.EditAlarm" />
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>